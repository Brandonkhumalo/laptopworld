import CartPage from "@/views/CartPage";

export const metadata = {
  title: "Your Cart | Laptop World",
  description: "Review your cart and proceed to checkout at Laptop World Zimbabwe.",
};

export default function Cart() {
  return <CartPage />;
}
