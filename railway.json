{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.railway",
    "watchPatterns": ["backend/**", "railway.json"]
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && echo 'Migrations complete, starting gunicorn on port '$PORT && gunicorn config.wsgi:application -c gunicorn.conf.py"
  }
}